<Page
    x:Class="pz23.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:pz23"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Style="{StaticResource MainGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Style="{StaticResource TopPanelGridStyle}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="UrlBox"
                     Grid.Column="0"
                     Style="{StaticResource UrlTextBoxStyle}"
                     KeyDown="UrlBox_KeyDown"/>

            <Button Grid.Column="1"
                    Style="{StaticResource GoButtonStyle}"
                    Click="Go_Click"/>

            <Button Grid.Column="2"
                    Style="{StaticResource StarButtonStyle}"
                    ToolTipService.ToolTip="Добавить в закладки"
                    Click="AddBookmark_Click"/>

            <DropDownButton x:Name="BookmarksButton"
                            Grid.Column="3"
                            Style="{StaticResource BookmarksButtonStyle}">
                <DropDownButton.Flyout>
                    <MenuFlyout x:Name="BookmarksFlyout" Placement="Bottom">
                        <MenuFlyoutItem Text="(пока пусто)" IsEnabled="False"/>
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>
        </Grid>

        <WebView x:Name="webView"
                 Grid.Row="1"
                 Style="{StaticResource WebViewStyle}"/>
    </Grid>
</Page>
