<Page
    x:Class="pz22.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:pz22"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Заказ кофе"
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Spacing="10">

                <TextBlock Text="Выберите кофе:" FontSize="18" FontWeight="SemiBold"/>

                <RadioButton Content="Americano (2.0€)"
                             GroupName="Coffee"
                             IsChecked="True"
                             Checked="Americano_Checked"/>

                <RadioButton Content="Latte (3.0€)"
                             GroupName="Coffee"
                             Checked="Latte_Checked"/>

                <RadioButton Content="Cappuccino (3.5€)"
                             GroupName="Coffee"
                             Checked="Cappuccino_Checked"/>

                <TextBlock Text="Размер:" FontSize="18" FontWeight="SemiBold" Margin="0,20,0,0"/>

                <RadioButton Content="Small (+0.0€)"
                             GroupName="Size"
                             IsChecked="True"
                             Checked="Small_Checked"/>

                <RadioButton Content="Medium (+0.5€)"
                             GroupName="Size"
                             Checked="Medium_Checked"/>

                <RadioButton Content="Large (+1.0€)"
                             GroupName="Size"
                             Checked="Large_Checked"/>

            </StackPanel>

            <StackPanel Grid.Column="1" Spacing="10">

                <TextBlock Text="Добавки:" FontSize="18" FontWeight="SemiBold"/>

                <CheckBox Content="Молоко (+0.5€)"
                          IsChecked="{Binding Milk, Mode=TwoWay}"/>

                <CheckBox Content="Сироп (+0.7€)"
                          IsChecked="{Binding Syrup, Mode=TwoWay}"/>

                <CheckBox Content="Доп. эспрессо (+1.2€)"
                          IsChecked="{Binding ExtraShot, Mode=TwoWay}"/>

            </StackPanel>

        </Grid>

        <Border Grid.Row="2"
                Margin="0,30,0,0"
                Padding="15"
                CornerRadius="10"
                Background="#22000000">

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10">
                <TextBlock Text="Итого:" FontSize="22" FontWeight="Bold"/>
                <TextBlock FontSize="22" FontWeight="Bold"
                           Text="{Binding TotalPrice}"/>
                <TextBlock Text="€" FontSize="22" FontWeight="Bold"/>
            </StackPanel>

        </Border>
    </Grid>
</Page>
